# Copy to .env and fill in only if you need it. .env is gitignored.
#
# wp-forge is fully LOCAL and needs NO secrets to run: it fetches public plugins
# from wordpress.org and reports locally (console + knowledge/<slug>/notifications.log).
#
# This file is intentionally almost empty. Add a key here only if you extend
# wp-forge to reach something that needs credentials.
